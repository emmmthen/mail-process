# Source Code

这里是前端应用源码入口。

## 文件职责

- `main.tsx`：应用启动入口
- `App.tsx`：路由与页面装配
- `index.css`：全局样式

## 目录结构

- `components/`：通用组件
- `pages/`：路由页面
- `services/`：接口访问与数据获取
- `types/`：共享类型定义

## 约定

- 页面负责编排，组件负责复用
- 接口调用不要散落在页面中
- 全局样式尽量收敛在入口样式文件

