# API Layer

这里存放 HTTP 路由定义。每个文件对应一个业务域，职责是接收请求、调用服务、返回响应。

## 当前路由

- `emails.py`：邮件导入与处理
- `quotes.py`：报价列表、查询、删除等接口
- `settings.py`：系统参数配置接口

## 编写约定

- 路由只做参数校验和编排
- 复杂业务逻辑下沉到 `services/`
- 响应结构统一使用 `schemas/`
- 接口命名保持资源导向

